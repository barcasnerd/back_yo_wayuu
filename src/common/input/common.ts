export enum InputType {
    CREATE,
    UPDATE,
    SEARCH,
    ACTION
}

export enum AvailableLogRecordEvenDataType {
    JSON = "json",
    STRING = "string",
    BOOL = "boolean"
}

export enum ValidLogRecordOwnerTypes {
    LIVING_COST = "livingCost",
    DRUG_CONFIGURATION = "drugConfiguration"
}